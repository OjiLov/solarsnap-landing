{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "git add -A 2>/dev/null; git diff --cached --quiet || (git commit -m \"auto: save changes $(date '+%Y-%m-%d %H:%M:%S')\" && git push origin 2>/dev/null); exit 0",
            "shell": "bash",
            "timeout": 30,
            "statusMessage": "Auto-committing to GitHub..."
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(git *)"
    ]
  }
}
